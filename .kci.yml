pipeline:
  build:
    image: index.qbox.me/wanglei/golang
    commands:
      - go build
  index.qbox.me/kci/plugin_docker:
    appenv: test
    repo: wanglei/justtest
    tag: latest
    file: Dockerfile
    insecure: false
    load: docker/image.tar
    save:
      destination: docker/image.tar
      tag: latest
    when:
      event: [push, pull_request, tag, deployment]
  index.qbox.me/kci/plugin_email:
    appenv: test
    recipients:
      - 541004974@qq.com
    when:
      event: [push, pull_request, tag, deployment]
      status:  [ failure, success ]
  index.qbox.me/kci/plugin_kodo:
    bucket: tumi
    source: /docker
    target: /docker
    access_key: 
    secret_key:

